# pytest config has moved to pyproject.toml [tool.pytest.ini_options].
# This file is kept for tox users only.
[tox]
envlist = py310, py311, py312

[testenv]
deps = -e .[dev]
commands_pre = python setup.py build_ext --inplace
commands = pytest {posargs}
