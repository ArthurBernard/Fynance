# Runtime dependencies — authoritative versions are defined in pyproject.toml.
# This file is kept for legacy compatibility (e.g. pip install -r requirements.txt).
matplotlib>=3.7
numba>=0.59
numpy>=2.0
polars>=1.0
scipy>=1.11
seaborn>=0.12
torch>=2.0
xgboost>=2.0
