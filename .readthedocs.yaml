version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  jobs:
    post_install:
      - python setup.py build_ext --inplace

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - doc

sphinx:
  configuration: doc/source/conf.py
  fail_on_warning: true
